query($gender: Gender) {
  degreesBySchool(gender: $gender) {
    type
    features {
      type
      geometry{
        type
        coordinates
      }
      properties {
        id	
        name
        degreeCount
        lon
        lat
      }
    }
  }
}