query($gender: Gender, $minDegree: Degree) {
  degreesBySchool(gender: $gender, minDegree: $minDegree) {
    type
    features {
      type
      geometry{
        type
        coordinates
      }
      properties {
        id	
        name
        degreeCount
        lon
        lat
      }
    }
  }
}